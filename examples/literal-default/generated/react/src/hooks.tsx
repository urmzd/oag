// Auto-generated by oag — do not edit
import useSWR, { type SWRConfiguration } from "swr";
import useSWRMutation, { type SWRMutationConfiguration } from "swr/mutation";
import { useApiClient } from "./provider.js";
import type { Message } from "./types.js";

export function useCreateMessage(
  config?: SWRMutationConfiguration<Message, Error, string, Message>,
) {
  const client = useApiClient();
  return useSWRMutation<Message, Error, string, Message>(
    "/messages",
    (_key: string, { arg }: { arg: Message }) => client.createMessage(arg),
    config,
  );
}

export function useGetMessage(id: string, config?: SWRConfiguration<Message>) {
  const client = useApiClient();
  return useSWR<Message>(["/messages/{id}", id] as const, () => client.getMessage(id), config);
}
